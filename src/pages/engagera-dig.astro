---
import type { Event } from '../events'
import Layout from '../layouts/Layout.astro';
import Headline from '../components/headings/Headline.astro'
import H2 from '../components/headings/H2.astro'
import * as eventsStore from '../events'

const title = "Engagera dig"
const events = await eventsStore.list()

const contacts = [
  { name: "Kim Burgestrand", how: "kim@burgestrand.se" },
  { name: "Anna-Lena M.W." },
  { name: "Sandra B." },
]

const adultsToSentence = (event: Event) => {
  const missing = Math.max(2 - event.data.adults.length, 0)
  const missingAdults = Array.from({ length: missing }).fill("🙋")
  return [...event.data.adults, ...missingAdults].join(", ")
}
---

<Layout title={title}>
  <header class="col-[full]">
    <Headline text={title} size={18.2} />
  </header>
  <main class="flex flex-col gap-y-16">
    <section class="flex flex-col gap-y-4">
      <H2>Vad gör man som vuxen på Rindögården?</H2>
      <p>
        Att vara vuxen på Rindögården innebär att du är med och skapar en trygg och rolig plats för barnen att vistas på. Du behöver inte vara någon expert på barn eller lek, utan det viktigaste är att du är närvarande och engagerad.
      </p>
      <p>
        Du är aldrig ensam vuxen på gården. Allra bäst blir det när man går ihop med någon man känner och bemannar en kväll tillsammans.
      </p>

      <div>
        <h3 class="font-bangers tracking-wider text-2xl leading-tight">Uppgifter</h3>
        <ul class="list-disc list-inside">
          <li>Var först på plats, möt barnen när de kommer</li>
          <li>För närvarolista och kontrollera medgivandeformulär</li>
          <li>Bemanna kiosken</li>
          <li>Hjälp till ifall något händer</li>
        </ul>
      </div>
    </section>

    <section class="flex flex-col gap-y-4">
      <H2>Kontakta oss</H2>
      <p>
        Vi är ett litet gäng vuxna som tillsammans sköter logistik och planering för Rindögården. Vi är alltid intresserade av att få in nya idéer och tankar, så tveka inte att höra av dig om du har något på hjärtat!
      </p>
      <ul class="flex flex-row flex-wrap gap-y-2 gap-x-4">
        {contacts.map(contact => (
          <li class="min-w-[max-content] flex-1 border rounded-md px-4 py-2">
            <h3 class="text-xl font-bangers tracking-widest">{contact.name}</h3>
            <p class="">{contact.how}</p>
          </li>
        ))}
      </ul>
    </section>

    <section class="flex flex-col gap-y-4">
      <H2>Tillfällen</H2>
      <p>
        Varje kväll behöver bemannas av minst två vuxna för att vi ska kunna hålla öppet. Nedan ser du vilka kvällar som är planerade och vilka som redan är bemannade.
      </p>

      <p>
        Kan du tänka dig att ställa upp? Underbart! Hör av dig till någon av oss i planeringsgruppen så lägger vi upp dig på schemat.
      </p>

      <table class="w-full my-6 font-mono text-sm mb-12">
        <thead>
          <tr class="text-left border-b-2">
            <th class="px-2">Datum & Tid</th>
            <th class="px-2">Tema & Vuxna</th>
          </tr>
        </thead>
        <tbody>
          {events.map((event) => (
            <tr class="data-[uncertain]:bg-red-600/40 even:bg-christalle-800 bg-transparent" data-uncertain={event.missingAdults > 0}>
              <td class="p-2">{event.date} kl.&nbsp;{eventsStore.time.tiny(event)}</td>
              <td class="p-2 flex flex-col">
                <strong>{event.data.title}</strong>
                <span>{adultsToSentence(event)}</span>
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </section>
  </main>
</Layout>