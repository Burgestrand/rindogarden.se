---
import { Image } from 'astro:assets';

const { image } = Astro.props
---

<article class="flex place-items-center place-content-center">
  <a href={image.src} class="inline-block"><Image src={image} width={420} height={280} alt="Affisch" class="rounded-md" /></a>
</article>
