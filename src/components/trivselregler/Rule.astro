---
interface Props {
  class?: string;
}
---

<li class:list={["flex items-baseline", Astro.props.class]}>
  <span class="text-puerto-rico-400 font-bold mr-3 mt-1">✓</span>
  <div class="flex flex-col gap-1">
    <div class="font-semibold">
      <slot name="rule" />
    </div>
    {Astro.slots.has("reason") &&
      <div class="text-sm text-gray-300">
        <slot name="reason" />
      </div>
    }
  </div>
</li>
