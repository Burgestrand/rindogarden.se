---
interface Props {
  title: string,
  [key: string]: any
}
const { title, ...rest } = Astro.props
---
<section>
  <header class="mb-4 bg-black p-2 rounded-xs">
    <h2 class="text-2xl">{title}</h2>
  </header>

  <div {...rest}>
    <slot />
  </div>
</section>